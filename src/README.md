# PolyWebNode
This project wires a MongoDB to models that duplicate what is in pathfinder assistant.
It uses Mongoose to connect to the MongoDB
Express for routing
/users - Users
/api - Pathfinder API

app.js contains main app
/routes contains express routes
/models contains dtos
gulp js helps maintain the app state when we make file changes


